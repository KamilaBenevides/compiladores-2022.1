program simples;
	var entrada, count, pot : integer;
			a, b : boolean;

begin
    a := 1;
    b := 1;
    pot := 1;
    read(entrada);

    for count := 0 to entrada do
        begin
        pot := pot * 10;
        
        end;

    write(pot);
end.